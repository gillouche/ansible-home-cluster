---
exclude_paths:
  - .venv/
  - collections/
  - .github/
  - node_modules/
  - roles/**/files/
  - roles/**/templates/
  - inventories/group_vars/*.vault.yml

# Keep some as warnings while iterating
warn_list:
  - yaml

skip_list:
  - var-naming[no-role-prefix]
  - internal-error
