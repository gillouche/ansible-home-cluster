---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true

- name: Restart Firefly
  ansible.builtin.systemd:
    name: "container-{{ firefly_container_name }}"
    state: restarted
    daemon_reload: true
  become: true
