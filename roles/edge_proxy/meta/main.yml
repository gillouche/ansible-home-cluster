---
galaxy_info:
  author: ansible-home-cluster
  description: Deploy and configure reverse proxy (Caddy) with Podman.
  license: MIT
  min_ansible_version: '2.13'
  platforms:
    - name: GenericLinux
      versions: ['all']
dependencies:
  - role: tls
  - role: keepalived
