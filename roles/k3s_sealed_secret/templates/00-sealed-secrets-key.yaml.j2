apiVersion: v1
kind: Secret
metadata:
  name: {{ sealed_secrets_key_name }}
  namespace: {{ sealed_secrets_namespace }}
  labels:
    sealedsecrets.bitnami.com/sealed-secrets-key: active
type: kubernetes.io/tls
data:
  tls.crt: {{ sealed_secrets_tls_crt }}
  tls.key: {{ sealed_secrets_tls_key }}
