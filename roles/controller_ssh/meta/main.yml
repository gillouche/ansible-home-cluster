---
galaxy_info:
  author: ansible-home-cluster
  description: Configure controller SSH (known_hosts, SSH config, bootstrap conveniences)
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: macOS
      versions: ["all"]
    - name: GenericLinux
      versions: ["all"]
dependencies: []
