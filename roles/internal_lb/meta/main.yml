---
galaxy_info:
  author: ansible-home-cluster
  description: Deploy and configure HAProxy with keepalived VIP for load balancer cluster.
  license: MIT
  min_ansible_version: '2.13'
  platforms:
    - name: GenericLinux
      versions: ['all']
dependencies:
  - role: keepalived
