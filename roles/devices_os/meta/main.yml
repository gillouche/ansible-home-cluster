---
galaxy_info:
  author: ansible-home-cluster
  description: Base OS setup for devices (locale, hostname, timezone, core dirs)
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: GenericLinux
      versions: ["all"]
dependencies: []
