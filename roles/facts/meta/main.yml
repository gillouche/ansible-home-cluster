---
galaxy_info:
  author: ansible-home-cluster
  description: Gather and print detailed system facts for debugging, enforce required variables (like target_user),
    and optionally end the play early after diagnostics.
  license: MIT
  min_ansible_version: '2.13'
  platforms:
    - name: MacOSX
      versions: ['all']
    - name: GenericLinux
      versions: ['all']
dependencies: []
