---
galaxy_info:
  author: ansible-home-cluster
  description: Bootstrap devices SSH (user ~/.ssh, keypair, controller key, inter-device trust)
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: GenericLinux
      versions: ["all"]
dependencies: []
