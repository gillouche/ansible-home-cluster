---
# ==============================================================================
# Homelab Cluster Maintenance Orchestrator
# ==============================================================================

# 1. Connectivity check
- name: Check connectivity
  import_playbook: includes/maintenance/ping.yml
  tags: [ping]

# 2. System updates (Pacman)
- name: Update system
  import_playbook: includes/maintenance/update.yml
  tags: [update]

# 3. Sequential staggered reboot
- name: Reboot hosts
  import_playbook: includes/maintenance/reboot.yml
  tags: [reboot]
