---
- name: Bootstrap controller host for SSH and network configuration
  hosts: localhost
  gather_facts: true
  become: false

  roles:
    - role: controller_network
      tags: [bootstrap, bootstrap_controller, bootstrap_controller_network]
    - role: controller_ssh
      tags: [bootstrap, bootstrap_controller, bootstrap_controller_ssh]
